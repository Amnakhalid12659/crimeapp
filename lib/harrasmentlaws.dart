import 'package:flutter/material.dart';

class HarrasmentlawScreen extends StatelessWidget {
  const HarrasmentlawScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Text("fffff"),
    );
  }
}
